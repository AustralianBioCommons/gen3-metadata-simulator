# Copy to .env and point LLM_API_KEY_FILE at a file containing your LLM API key.
# The .env file holds the PATH to the key file, never the key itself.
# .env is gitignored; the key file should live outside the repo.
LLM_API_KEY_FILE=/path/to/your/anthropic_key
